import React from 'react'
import "./styles.scss";
const FirstAuthFormWrapper = ({children}) => {
    return (
        <div className='firstAuthFormWrapper'>
            {children}
        </div>
    )
}

export default FirstAuthFormWrapper
